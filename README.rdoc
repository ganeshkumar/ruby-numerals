== README

To Run the app

1) bundle install
2) bundle exec rake db:create
3) bundle exec rake db:migrate
4) bundle exec rails s

http://localhost:3000

5) To run test cases
# MiniTest is a fast and lightweight testing library
#
bundle exec rake minitest:controllers